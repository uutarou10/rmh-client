import * as React from 'react';
import Header from '../../Header';
import RequestForm from './RequestForm';

const Main =  () => {
  return (
    <React.Fragment>
      <Header />
      <RequestForm />
    </React.Fragment>
  );
};

export default Main;